<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mg.noteappfirebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".send_email" />
        <activity android:name=".change_password" />
        <activity android:name=".MainActivity" />
        <activity android:name=".create_account">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".log_in" />
        <activity android:name=".CRUD.create_note" />
        <activity android:name=".CRUD.view_note" />
        <activity android:name=".CRUD.update_note" />
    </application>

</manifest>